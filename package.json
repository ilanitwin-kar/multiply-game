{
  "name": "multiply-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "npx --yes serve www -p 5173",
    "dev": "npx --yes serve www -p 5173",
    "tunnel": "npx --yes cloudflared tunnel --url http://127.0.0.1:5173",
    "cap:init": "npx cap init",
    "android": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
